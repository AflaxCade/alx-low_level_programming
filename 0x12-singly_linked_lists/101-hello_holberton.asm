#include <stdio.h>

/**
 * main - Prints a message before the main function is executed
 * Return: Always 0
 */
int main(void)
{
	printf("You're beat! and yet, you must allow,\n"
	       "I bore my house upon my back!\n");
	return (0);
}
